{
  "name": "funnel-server",
  "version": "1.0.0",
  "description": "Funnel API server",
  "scripts": {
    "test": "deno test tests/",
    "test:unit": "deno test tests/api_test.ts",
    "test:integration": "deno test tests/integration_test.ts --allow-env --allow-read --allow-net",
    "promptfoo:eval": "cd promptfoo && npm run promptfoo:eval",
    "promptfoo:view": "cd promptfoo && npm run promptfoo:view",
    "promptfoo:install": "cd promptfoo && npm install"
  },
  "dependencies": {
    "ws": "^8.18.2"
  }
}
